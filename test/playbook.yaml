---

- hosts: TestServers
  sudo: yes

  tasks:  
  
  - name: resolving dependency for libfontconfig
    apt: name=libfontconfig state=present
  
  - name: run test
    shell: mvn test
    args:
      chdir: /avivaTest/
    ignore_errors: yes
